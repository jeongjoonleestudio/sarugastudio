# sarugastudio
saruga studio webpage

## Home info box

The yellow homepage info box reads from the Google Sheet URL in `projects.js`.
Create a published sheet tab named `home_info` with a `content` column. Edit the
text in that column to update the yellow box without changing the code.
